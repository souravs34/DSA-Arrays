// Brute force

// Using set

set<int> st;
for(){
 st.insert(arr[i]);  // TC -> NLogN
}
int index=0;
for(auto it:st){
    arr[index] = it;  // TC -> N
    index++;
}

// TC -> LogN 
// SC -> O(N)


// Optimised solution -> Two pointer

